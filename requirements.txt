# WAgent 阶段一后端依赖清单
# 与 docs/15-阶段一-核心设计任务实施规范.md §4 保持一致
# 安装：pip install -r requirements.txt

# ---- Web 框架与运行时（C2 工具 Schema）----
fastapi>=0.115
uvicorn[standard]>=0.30

# ---- 数据契约（C1 核心任务：Pydantic 模型为唯一事实来源）----
pydantic>=2.9
pydantic-settings>=2.5

# ---- 数据模型与存储（C3 核心任务：ORM + 迁移 + PostgreSQL）----
sqlalchemy>=2.0
alembic>=1.13
asyncpg>=0.29
redis>=5.0

# ---- 调度与 HTTP 客户端（C4 降级语义：重试/超时/源系统调用）----
apscheduler>=3.10
httpx>=0.27
tenacity>=8.5

# ---- 会话身份与 LLM（C2：student_id 注入 + Function Calling）----
pyjwt>=2.9
openai>=1.40

# ---- 观测与审计（C3 审计日志：全链路 Trace + 结构化脱敏日志）----
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
structlog>=24.4

# ---- 测试（验收标准 AC：时间冻结测 TTL/is_stale、mock 源系统超时）----
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=5.0
freezegun>=1.5
respx>=0.21
